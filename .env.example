# Neo4j (local Docker or Aura)
# Aura: use instance URI, instance ID as user, and password from Aura console
NEO4J_URI=neo4j+s://YOUR_INSTANCE_ID.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=
# Optional: Aura sometimes uses instance ID as database name
# NEO4J_DATABASE=neo4j

# Local Postgres (Docker) — host port 5433 to avoid conflict with local Postgres on 5432
# Required by ./scripts/up.sh for the Postgres container (must be non-empty)
POSTGRES_PASSWORD=
# POSTGRES_URL=postgres://claidex:YOUR_PASSWORD@localhost:5433/claidex
POSTGRES_URL=

# Neon (cloud Postgres) — use pooled connection strings for production
# Use postgresql:// URLs with ?sslmode=require (or verify-full for stricter verification).
# In Neon Console: Connection details → enable "Connection pooling" to get the pooler host
# (host contains -pooler, e.g. ep-xxx-pooler.region.aws.neon.tech). See:
# https://neon.com/docs/connect/connection-pooling
# API uses one DB: set NEON_PROVIDERS_URL (or DATABASE_URL) to the project that has
# providers, provider_risk_scores, and exclusions (e.g. claidex-providers).
NEON_PROVIDERS_URL=
NEON_PAYMENTS_URL=
NEON_ENTITIES_URL=
NEON_EXCLUSIONS_URL=

# Cloudflare R2
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT=

# Upstash Redis
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# External APIs
OPENCORPORATES_API_KEY=
SAM_GOV_API_KEY=

# App
NODE_ENV=development
# Port for API when run locally (Docker API uses 4001)
PORT=4002

# Settings / Me API: current user for /v1/me (dev only). Use seed user UUID, e.g. aaaaaaaa-0000-4000-8000-000000000002
# DEV_USER_ID=aaaaaaaa-0000-4000-8000-000000000002
# NEXT_PUBLIC_DEV_USER_ID=aaaaaaaa-0000-4000-8000-000000000002

# Frontend: API base URL (use when running Next.js locally)
# Docker: use http://localhost:4001 — Local api: use http://localhost:4002
NEXT_PUBLIC_API_BASE_URL=http://localhost:4001
NEXT_PUBLIC_API_URL=http://localhost:4001

# API (production): set in Render/Railway to allow frontend origin(s)
# CORS_ORIGIN=https://your-app.vercel.app
